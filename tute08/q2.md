```mermaid
sequenceDiagram
    actor Client
    participant AuthenticatorService
    activate Client
    Client->>AuthenticatorService: login(username, password)
    activate AuthenticatorService
    AuthenticatorService-->>Client: Login Success
    deactivate AuthenticatorService
    deactivate Client
```